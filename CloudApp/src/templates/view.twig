{% include 'header.twig' %}

<p><a href="{{ app.request.baseUrl ~ '/add' }}" class="btn"><b class="icon-pencil"></b> Sign In</a></p>

<p><a href="{{ app.request.baseUrl ~ '/view' }}" class="btn"><b class="icon-pencil"></b> See Attendance</a></p>
<style>
  table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
  }

    td, th {
      border: 1px solid #dddddd;
      text-align: left;
      padding: 8px;
    }

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>

<table>
            <tr>
                <th> Date </th>
                <th> Name </th>
                <th> Location </th>
            </tr>

        {%  for element in list %}
            <tr>
                <td> {{element.date1}} </td>
                <td> {{element.name}} </td>
                <td> {{element.location}} </td>
            </tr>
        {%  endfor %}
      </table>


{% include 'footer.twig' %}
